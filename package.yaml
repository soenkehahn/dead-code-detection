name: find-dead-code

ghc-options: -Wall -fno-warn-name-shadowing

executables:
  find-dead-code:
    main: Main.hs
    dependencies:
      - ghc-paths
      - uniplate
      - graph-wrapper

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - hspec
      - mockery
      - interpolate
